<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetMobile">
        <activity android:name=".NoteCustomList"></activity>
        <activity android:name=".DevoirCustomList" />
        <activity android:name=".BudgetActivity" />
        <activity android:name=".VDCActivity" />
        <activity android:name=".ConnexionActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".AccueilCoActivity" />
        <activity android:name=".ActualiterCustomList" />
        <activity android:name=".EDTActivity" />
        <activity android:name=".EDTCustomList" />
        <activity android:name=".AccueilActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>